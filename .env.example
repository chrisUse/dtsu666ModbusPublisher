# DTSU666 Modbus to MQTT Reader - Environment Configuration
# Copy this file to '.env' and adapt the values to your environment:
# cp .env.example .env

# -------------------------
# MODBUS CONFIGURATION
# -------------------------
# USB-RS485 Adapter Device (often /dev/ttyUSB0, /dev/ttyACM0, or /dev/ttyAMA0)
# Find your device with: dmesg | grep tty
MODBUS_PORT=/dev/ttyACM0

# Modbus Slave ID of the DTSU666 (default: 1, check device documentation)
MODBUS_SLAVE=1

# Baudrate for Modbus RTU (default: 9600)
BAUDRATE=9600

# Timeout for Modbus requests in seconds
TIMEOUT=1.0

# -------------------------
# MQTT BROKER CONFIGURATION  
# -------------------------
# IP address or hostname of your MQTT broker
MQTT_BROKER=192.168.1.100

# MQTT Port (default: 1883 for unencrypted, 8883 for TLS)
MQTT_PORT=1883

# MQTT username
MQTT_USER=your_mqtt_user

# MQTT password
MQTT_PASS=your_mqtt_password

# MQTT topic prefix for all sensors
MQTT_PREFIX=chint/dtsu666

# -------------------------
# HOME ASSISTANT CONFIGURATION
# -------------------------
# Device name as displayed in Home Assistant
DEVICE_NAME=Chint DTSU666

# Unique device ID (only a-z, 0-9, _ allowed)
DEVICE_ID=dtsu666_meter

# -------------------------
# MEASUREMENT INTERVAL
# -------------------------
# Time between measurements in seconds (recommended: 5-60)
MEASUREMENT_INTERVAL=5

# -------------------------
# EXAMPLE CONFIGURATIONS
# -------------------------
# Raspberry Pi with USB-RS485 adapter:
# MODBUS_PORT=/dev/ttyUSB0
# BAUDRATE=9600

# Home Assistant (local):
# MQTT_BROKER=homeassistant.local
# MQTT_PORT=1883
# MQTT_USER=mqtt_user
# MQTT_PASS=secure_password

# Mosquitto Broker:
# MQTT_BROKER=192.168.1.50
# MQTT_PORT=1883

# Common RS485 adapter devices:
# MODBUS_PORT=/dev/ttyUSB0    # CH340/CH341 USB-Serial
# MODBUS_PORT=/dev/ttyACM0    # CDC ACM devices  
# MODBUS_PORT=/dev/ttyAMA0    # Raspberry Pi GPIO UART